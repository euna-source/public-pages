# GA 화면명 통일 · 결정 메모

2026-09-12 · 기준: AOS develop 9/10(C8Screen 63개) · iOS 화면 목록 9/10(232행, 현행 17) · 정의서 v2 9/9(178개)

## 결정

1. GA에 보내는 `screen_name` 값은 영문 snake_case 키로 확정합니다. 한국어 제목은 대장과 대시보드의 라벨 층에만 둡니다.
2. 이미 발송 중인 이름(AOS 63개·iOS 17개)은 바꾸지 않습니다. 두 화면이 한 키를 두고 다투면 개명이 아니라 새 키를 추가합니다. 예외는 하나, AOS `my_page`는 `my_profile_card`와 같은 순간에 나가는 중복이라 멈춥니다.
3. 화면은 "사용자가 전면에서 읽거나 입력하는 목적지"와 "독립 과업을 하는 시트"입니다. 시스템 피커·확인/완료 팝업·옵션 시트·정렬 시트·컨테이너·QA 도구는 화면이 아닙니다. 하위탭은 경로 분석에서 갈라 봐야 하면 이름으로, 같은 목록의 필터면 `tab` 파라미터로 나눕니다.

## 수치

| 항목 | 값 |
|---|---|
| 통합 화면 | 170개 (핵심 31개는 1차 실수신 검증 대상) |
| AOS 현행 63개 | 유지 62 · 중복 제거 1(`my_page`) · 체류 추가 권고 12 · 이름만→발송 2 |
| AOS 새로 낼 이름 | 추가 73 · 존재 확인 후 결정 23 |
| iOS 232행 | 화면 채택 187 · 제외 45 |
| iOS 구현 | 현행 유지 17 · 새 발송 구현 140개 화면 · 확인·보류 11 |

## 기각한 안과 대가

- 한국어 제목을 GA 값으로 보내는 안(정의서 v2)은 접습니다. 발송 중인 80개 이름을 모두 바꿔 지표 역사가 끊기고, 식별자에 언어를 섞게 됩니다. 대신 GA 화면 보고서에는 영문 키가 보이므로 라벨 층(대시보드 매핑표)의 주인이 필요합니다.
- iOS 232행 전부에 이름을 붙이는 안은 접습니다. 화면 수가 부풀어 세션당 화면 수·체류·전환이 흐려집니다.
- 의미가 어색한 현행명 3개(`my_page_menu`=설정 홈, `user_resume`=내 이력 관리, `member_profile_inspiration`=타인 프로필 인스퍼레이션 탭)는 바꾸지 않고 라벨로 바로잡습니다.

## 뒤집힐 조건

- 라벨 층 주인이 정해지지 않아 GA 화면을 영문 키 그대로 읽는 사람이 다수가 되면 한국어 값을 다시 검토합니다.
- AOS 63개 지표를 실제로 읽는 사람이 없다고 확인되면 역사 보존 논거가 사라지므로 위 3개 이름을 바로잡을 수 있습니다.
- `contacts_team`(AOS)과 `team_list`(iOS)가 같은 화면으로 확인되면 대시보드 별칭으로 합치고, 다음 큰 개편에서 하나로 수렴합니다.

## 담당(DRI)

| 역할 | 담당 | 할 일 |
|---|---|---|
| 기획 리드 | (기입) | 대장(`screen-keys.json`)의 주인으로 추가·변경을 승인합니다. |
| AOS 리드 | (기입) | `C8Screen` enum을 대장에 맞춰 갱신, `my_page` 중지, 진입만 12개 화면 이탈 발송 추가, 추가 73개를 구현합니다. |
| iOS 리드 | (기입) | 화면 이름 enum 신설(문자열 리터럴 금지), 파라미터 4종(screen_id·previous_screen·entry_point·tab) 추가, 새 발송 140개 화면을 구현합니다. |
| 데이터 담당 | (기입) | 라벨 층 매핑표 소유, DebugView 실수신 검증, 구이름(`my_page`)을 별칭으로 처리합니다. |

## 다음 행동

| 언제 | 누가 | 무엇 |
|---|---|---|
| 9/15(월) | 기획 리드 | 이 메모와 대장을 AOS·iOS 리드에게 보냅니다. |
| 9/17(수) | AOS·iOS 리드 | 행별 유지·추가·제외 회신과 "확인" 표시 화면의 존재 여부 회신. 키 파일 저장 위치 합의. |
| 구현 후 첫 알파 | 데이터 담당 | 핵심 31개 화면을 양 OS DebugView에서 실수신 확인. 뒤로가기·탭 전환·시트 닫힘 재발송 규칙이 두 OS에서 같은지 확인. |
| 확인 뒤 | 기획 리드 | 대장 상태를 "수집 검증 완료"로 올리고 라벨 층 매핑표를 대시보드에 연결합니다. |
